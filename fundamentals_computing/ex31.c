#include <stdio.h>

int main(){

  printf("enter a string: ");
  scanf("%s", str);

  printf("%s", str);

  char link[30]
  printf("enter a line: ");
  fgets(line, 20, stdin);
  printf("%s\n", line);

  return 0;

}
